#!/bin/sh
# The Tests workflow runs ruff before pytest, so a lint error fails CI with the
# suite never having run. Catch it here instead of on GitHub.
# Enable once per clone: git config core.hooksPath .githooks
set -e
uv run ruff check
uv run pytest -q
